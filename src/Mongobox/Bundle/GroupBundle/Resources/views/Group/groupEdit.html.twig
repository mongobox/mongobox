{% extends "MongoboxGroupBundle:Group:layout.html.twig" %}

{% form_theme form 'MongoboxJukeboxBundle:Form:mongobox_form_theme.html.twig' %}

{% block body %}
        
	<h1>Edition d'un groupe</h1>
	<form action="{{ path('group_edit', {'id' : group.id}) }}" class="content-block form-inline" method="POST" {{ form_enctype(form) }} novalidate>
		{{ form_widget(form) }}

		<hr />
		<div>
			<input type="submit" class="btn btn-primary pull-right" value="Créer">
		</div>
	</form>
	<h2>Utilisateurs du groupe</h2>
	<div class="content-block">
		{% for user in group.users %}
			{% include "MongoboxUsersBundle:User/Avatar:avatar.html.twig" %}
		{% endfor %}
	</div>
{% endblock body %}