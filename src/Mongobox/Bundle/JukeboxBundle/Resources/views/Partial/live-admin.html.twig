<div class="tab-pane" id="playlist_controll">
	<h3>Gestion de la playlist</h3>
	<table id="list_tags">
		<thead>
			<th>Tags</th>
			<th>Nombre de vidéos</th>
			<th>Actions</th>
		</thead>
		<tbody>
		{% for tag in list_tags %}
			<tr>
				<td>{{tag[0].name}}</td>
				<td>{{tag.total}}</td>
				<td>
					{% include 'MongoboxJukeboxBundle:Partial:live-button-tag-video.html.twig' with {'tag' : tag[0], 'group' : group} %}
				</td>
			</tr>
		{% endfor %}
		</tbody>
	</table>
	{% include 'MongoboxJukeboxBundle:Partial:live-list-tag.html.twig' with {'player_mode' : player_mode, 'groupLiveTags' : groupLiveTags} %}
</div>

{% include 'MongoboxJukeboxBundle:Partial:replaceVideo.html.twig' %}

<script type="text/javascript">
    var nextVideoUrl	    = '{{ path('live_next') }}';
    var replaceUrl          = '{{ path('live_replace') }}';
    var putschUrl           = '{{ path('live_putsch') }}';
    var putschResponseUrl   = '{{ path('live_putsch_response') }}';
    var worstScoreAllowed   = -{{ max_dislikes }};
</script>

<link href="{{ asset('bundles/mongoboxjukebox/css/admin.css') }}" rel="stylesheet">
<script src="{{ asset('bundles/mongoboxjukebox/js/live-admin.js') }}"></script>
<script src="{{ asset('bundles/mongoboxjukebox/js/liveManageTags.js') }}"></script>
