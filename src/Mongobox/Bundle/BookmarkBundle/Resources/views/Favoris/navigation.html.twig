<ul class="nav nav-tabs">
	<li {% if app.request.get('_route') == 'user_voir_favoris' %}class="active"{% endif %}>
		<a href="{{ path('user_voir_favoris') }}" title="Voir vos favoris">Vos favoris ({{ nombre_favoris }})</a>
	</li>
	<li {% if app.request.get('_route') == 'user_voir_listes' %}class="active"{% endif %}>
		<a href="{{ path('user_voir_listes') }}" title="Voir vos listes">Vos listes (<span id="lists_number">{{ nombre_listes }}</span>)</a>
	</li>
	<li {% if app.request.get('_route') == 'import_favoris_index_action' or app.request.get('_route') == 'import_action' %}class="active"{% endif %}>
		<a href="{{ path('import_favoris_index_action') }}">Importer les favoris dans un groupe</a>
	</li>
	<li class="pull-right">
		<a href="#modal_add_list" title="Créer une liste" class="btn btn-default" data-toggle="modal">Créer une liste</a>
	</li>

	<div class="modal hide fade" id="modal_add_list">
		<div class="modal-header">
			<button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
			<h3>Création d'une liste</h3>
		</div>
		<div class="modal-body">
			<div class="input-group">
				<span class="input-group-addon"><i class="glyphicon glyphicon-pencil"></i></span>
				<input class="col-md-2" id="add-list-name" type="text" placeholder="Le titre de votre liste">
			</div>
		</div>
		<div class="modal-footer">
			<button type="button" class="btn btn-default" data-dismiss="modal" aria-hidden="true">Annuler</button>
			<a id="btn-submit-list" data-route="{{ app.request.attributes.get('_route') }}" href="{{ path('add_new_list') }}" class="btn btn-primary" data-loading-text="<img src='{{ asset('bundles/mongoboxbookmark/img/ajax-loader-list.gif') }}' alt='Chargement' />">Créer la liste</a>
		</div>
	</div>
</ul>
