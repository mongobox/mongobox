
{% block body -%}
    <h1>Voter pour un resto</h1>

    <form action="{{ path('vote_create',{'id':entity.decision.id}) }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

{% endblock %}
